version: "3.9"

services:
  bot:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-sqlite:////app/data/affiliate.db}
    volumes:
      - sqlite_data:/app/data
    restart: unless-stopped

  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: splref
  #     POSTGRES_USER: splref
  #     POSTGRES_PASSWORD: splref
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pg_data:/var/lib/postgresql/data

volumes:
  sqlite_data:
  # pg_data:
